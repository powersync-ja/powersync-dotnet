<Page x:Class="UnoTodo.Presentation.SqlConsolePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UnoTodo.Presentation"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>

    <utu:NavigationBar Content="SQL Console" />

    <StackPanel Grid.Row="1" Spacing="10" Padding="16,8">
      <TextBox Text="{Binding Query, Mode=TwoWay}"
               PlaceholderText="Enter SQL query..." />
      <Button Content="Execute Query"
              Command="{Binding Execute}"
              HorizontalAlignment="Left" />
    </StackPanel>

    <ScrollViewer Grid.Row="2" Padding="16,0,16,16">
      <StackPanel Spacing="10">
        <TextBlock Text="{Binding Headers}"
                   FontWeight="Bold"
                   TextWrapping="Wrap" />
        <TextBlock Text="{Binding Results}"
                   FontFamily="Consolas"
                   TextWrapping="Wrap" />
      </StackPanel>
    </ScrollViewer>
  </Grid>
</Page>
